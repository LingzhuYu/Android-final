<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 按下的效果 -->
    <item android:state_pressed="true">
        <shape>
            <!-- gradient表示渐变，startColor是起始色，endColor是结束色，
                 type是线性渐变类型(默认类型)，angle是渐变角度 -->
            <gradient android:startColor="#555556" android:endColor="#8f9091"
                android:type="linear" android:angle="90" />
            <!-- stroke是边缘颜色，width表示边缘宽度，color是边缘颜色 -->
            <!-- stroke可用于实现边缘模糊，反光等效果 -->
            <stroke android:width="2dip" android:color="#222223" />
        </shape>
    </item>

    <!-- 默认效果 -->
    <!-- 注意：默认效果没有stroke，而按钮效果有stroke；这样可以实现类似按下的动态效果 -->
    <item>
        <shape>
            <gradient android:startColor="#1e1e1e" android:endColor="#121111"
                android:type="linear" android:angle="90"/>
            <stroke android:width="1dp" android:color="#161616" />
        </shape>
    </item>
</selector>